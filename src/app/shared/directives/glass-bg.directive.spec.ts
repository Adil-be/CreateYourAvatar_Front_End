import { GlassBgDirective } from './glass-bg.directive';

describe('GlassBgDirective', () => {
  it('should create an instance', () => {
    const directive = new GlassBgDirective();
    expect(directive).toBeTruthy();
  });
});
